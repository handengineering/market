import { styled } from "~/styles/stitches.config";

const Main = styled("main", {
  display: "flex",
  flexDirection: "column",
  maxWidth: "1200px",
  alignSelf: "center",
  padding: "$5",
  flex: 1
});

export default Main;
